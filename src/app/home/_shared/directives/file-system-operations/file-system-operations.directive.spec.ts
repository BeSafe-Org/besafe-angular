import { FileSystemOperationsDirective } from './file-system-operations.directive';

describe('FileSystemOperationsDirective', () => {
  it('should create an instance', () => {
    const directive = new FileSystemOperationsDirective();
    expect(directive).toBeTruthy();
  });
});
